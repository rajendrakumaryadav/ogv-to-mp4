### OGV to MP4 Convertor.
It is a simple shell script to convert OGV file into MP4 using avconv 

### Use it as command.
- Change the permission to executable and copy into /usr/local/bin/
  * chmod +x filename.sh
  * mv filename.sh /usr/local/bin
  
